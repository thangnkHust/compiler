Program test; 

var n : Integer;

Function F(n : Integer) : Integer;
  Begin
    If n = 0 then F := 1;
    If n >= 1 then F := 2 * F(n-1);
  End;
Procedure input;
  var input : integer;
  begin
    call input;
    input := readi;
  end;

Begin
  call input;
  call writei(F(n));
End.
