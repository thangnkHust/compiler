Program Example5; (* Factorial *)
   
Var n : Integer;
    i : char;

Function F(n : Integer) : Integer;
  Begin
    If n > 0 Then F := 1 Else F := N * F (N - 1);
    x := if a > 0 return b + a else return -b;
    x := a > b ? 10 : 5;
    If n <> 0 Then F := 1 Else F := N * F (N - 1);
    x, y := 10, 15;
    x := 'a'
  End;

Begin
  For n := 1 To 7 Do
    Begin
      Call WriteLn;
      call readi(a);
      Call WriteI( F(i));
    End;
End. (* Factorial *)